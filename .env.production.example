# Production env template for Vercel. Set these in Vercel project settings
# (Dashboard → Settings → Environment Variables).

# URL of your deployed Knowledge Portal API on Render.
VITE_API_URL=https://knowledge-portal-api.onrender.com
